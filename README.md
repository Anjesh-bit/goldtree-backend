# GoldTree Backend 🌳

A simple, scalable backend API built with Node.js and Express — designed for local development with features like auto-reload via `nodemon`.

## 🚀 Features

- RESTful API built on **Express.js**
- Auto‑reload during development using **nodemon**
- Structured project layout (controllers, services, routers, middleware)
- DB connection through `db/` (e.g., MongoDB, PostgreSQL)
- Organized configs, constants, utility modules

## 🧾 Prerequisites

- **Node.js** v16+ (includes npm)
- Database (e.g., MongoDB, PostgreSQL) depending on your config

## 🔧 Installation

```bash
git clone https://github.com/Anjesh-bit/goldtree-backend.git
cd goldtree-backend
npm install